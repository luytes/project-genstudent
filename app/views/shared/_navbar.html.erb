<div class="navbar-fixed" style="background-color: #2B363F">
  <!-- Logo -->
  <%= link_to root_path, class: "navbar-wagon-brand" do %>
    <div class="genstudent-logo">
      <h3 class="gen-logo">GENSTUDENT</h3>
    </div>
  <% end %>

  <!-- Right Navigation -->
  <div class="navbar-wagon-right hidden-xs hidden-sm">

    <!-- Different navigation if login or not -->
    <% if user_signed_in? %>

      <!-- Profile picture and dropdown -->
      <div class="navbar-wagon-item">
        <div class="dropdown">
          <%= image_tag current_user.profile_picture || "http://placehold.it/30x30", class: "avatar dropdown-toggle", id: "navbar-wagon-menu", "data-toggle" => "dropdown" %>

          <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">

            <!-- If admin -->
            <% if current_user.admin == true %>
              <li>
                <%= link_to dashboard_path do %>
                  <i class="fa fa-home"></i>  <%= t(".profile", default: "Dashboard") %>
                <% end %>
              </li>
            <% else %>
              <li>
                <%= link_to dashboard_path do %>
                  <i class="fa fa-user"></i> <%= t(".profile", default: "My Account") %>
                <% end %>
              </li>
            <% end %>
            <li>
              <%= link_to destroy_user_session_path, method: :delete do %>
                <i class="fa fa-sign-out"></i>  <%= t(".sign_out", default: "Log out") %>
              <% end %>
            </li>
          </ul>
        </div>
      </div>

    <% else %>

      <%= link_to t(".browse_services", default: "Browse Services"), services_path, class: "navbar-wagon-item navbar-wagon-link deletethis text-center" %>
      <%= link_to t(".submit_task", default: "Submit A Task"), new_survey_path, class: "navbar-wagon-item navbar-wagon-link deletethis text-center" %>

      <div class="navbar-wagon-item">
        <div class="dropdown">
          <p class="dropdown-toggle navbar-wagon-item navbar-wagon-link no-margin-bot navbar-button text-center" data-toggle="dropdown">Sign Up</p>
          <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
            <li><%= link_to t(".sign_up", default: "As a company"), new_user_registration_path %></li>
            <!-- <li><#%= link_to t(".sign_up", default: "As a student"), new_user_registration_path %></li> -->
        </div>
      </div>

      <!-- Login text link -->
      <%= link_to t(".sign_in", default: "Login"), new_user_session_path, class: "navbar-wagon-item navbar-wagon-link deletethis text-center" %>

    <% end %>

    <!-- Button (call-to-action) -->
<!--     <%= link_to t(".top_call_to_action", default: "Post stuff"), "#", class: "navbar-wagon-item navbar-button btn" %>
 -->
 </div>

  <!-- Dropdown list appearing on mobile only -->
  <div class="navbar-wagon-item hidden-md hidden-lg">
    <div class="dropdown">
      <i class="fa fa-bars dropdown-toggle" data-toggle="dropdown"></i>
      <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
        <li><%= link_to t(".sign_in", default: "Login"), new_user_session_path %></li>
        <li><%= link_to t(".sign_up", default: "Sign Up"), new_user_registration_path %></li>
        <li><%= link_to t(".submit_task", default: "Submit A Task"), new_survey_path %></li>
      </ul>
    </div>
  </div>
</div>

<script>
  /* When the user clicks on the button,
  toggle between hiding and showing the dropdown content */
  function selectType() {
      document.getElementById("signup-choose").classList.toggle("show");
  }

  // Close the dropdown menu if the user clicks outside of it
  window.onclick = function(event) {
    if (!event.target.matches('.dropbtn')) {

      var dropdowns = document.getElementsByClassName("dropdown-content");
      var i;
      for (i = 0; i < dropdowns.length; i++) {
        var openDropdown = dropdowns[i];
        if (openDropdown.classList.contains('show')) {
          openDropdown.classList.remove('show');
        }
      }
    }
  }
</script>
